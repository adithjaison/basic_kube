services:
  basic_kube:
    container_name: basic_kube_dev
    build:
      context: ../
      dockerfile: ./docker/basic_kube/Dockerfile
      target: dev
    stdin_open: true
    tty: true
    volumes:
      - ../:/basic_kube
    command: /scripts/start_runserver.sh