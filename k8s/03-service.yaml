apiVersion: v1
kind: Service
metadata:
  name: hello-grpc
  namespace: basic-kube
  labels:
    app.kubernetes.io/name: hello-grpc
    app.kubernetes.io/part-of: basic-kube
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hello-grpc
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
